# API base path. With the Vite dev proxy this stays "/api" (same-origin).
# In production it can be an absolute URL to the gateway/CloudFront.
VITE_API_URL=/api
# Dev-only: where the Vite proxy forwards /api requests.
VITE_API_TARGET=http://localhost:8080
